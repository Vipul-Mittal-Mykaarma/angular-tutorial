import { TwoInputDirectiveDirective } from './two-input-directive.directive';

describe('TwoInputDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TwoInputDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});

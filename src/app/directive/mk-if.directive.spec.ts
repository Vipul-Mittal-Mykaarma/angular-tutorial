import { MkIfDirective } from './mk-if.directive';

describe('MkIfDirective', () => {
  it('should create an instance', () => {
    const directive = new MkIfDirective();
    expect(directive).toBeTruthy();
  });
});

import { MkIf2Directive } from './mk-if-2.directive';

describe('MkIf2Directive', () => {
  it('should create an instance', () => {
    const directive = new MkIf2Directive();
    expect(directive).toBeTruthy();
  });
});
